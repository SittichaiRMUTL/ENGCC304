#include <stdio.h>

int main() {
    
    char OS[30], Editor[30];

    scanf("%s", OS);
    scanf("%s", Editor);

    printf("OS: %s\n", OS);
    printf("Editor: %s\n", Editor);
    printf("Ready to code!\n");

    return 0;
}